'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { api } from '@/lib/api';
export default function Login(){const router=useRouter();const[email,setEmail]=useState('gov@reliefchain.demo');const[password,setPassword]=useState('Relief@123');const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');try{const result=await api<any>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('reliefchain-token',result.accessToken);localStorage.setItem('reliefchain-user',JSON.stringify(result.user));router.push(result.user.role==='AUDITOR'?'/auditor':'/operator')}catch(e){setError(e instanceof Error?e.message:'Login failed')}}return <main className="auth-wrap"><div className="card auth-card"><a className="brand" href="/"><span className="brandmark">R</span> ReliefChain</a><h1>Welcome back.</h1><p className="muted">Secure access for relief operators and oversight teams.</p><form onSubmit={submit}><div className="field"><label>Email address</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password"/></div>{error&&<p className="error">{error}</p>}<button className="button" style={{width:'100%'}} type="submit">Continue securely</button></form><p className="hint">Demo accounts: gov@reliefchain.demo, ngo@reliefchain.demo, or auditor@reliefchain.demo<br/>Password: Relief@123</p></div></main>}
