import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Backgrounds
  static const background = Color(0xFFEAF5FF);
  static const backgroundLight = Color(0xFFF4FAFF);

  // Brand
  static const primary = Color(0xFF0759B8);
  static const primaryLight = Color(0xFFE3F0FF);
  static const primarySoft = Color(0xFFE6F1FF);

  // Text
  static const navy = Color(0xFF0A1E44);
  static const muted = Color(0xFF64748B);
  static const placeholder = Color(0xFF9AA7B6);

  // Surfaces
  static const white = Colors.white;
  static const border = Color(0xFFD6E3EF);
  static const divider = Color(0xFFE6EDF5);

  // Status
  static const success = Color(0xFF25864B);
  static const successBackground = Color(0xFFE3F7EA);

  static const pending = Color(0xFF0759B8);
  static const pendingBackground = Color(0xFFE6F1FF);

  static const failed = Color(0xFFC33F53);
  static const failedBackground = Color(0xFFFFE8EC);

  static const reversed = Color(0xFFAF6912);
  static const reversedBackground = Color(0xFFFFF0DB);

  static const unknown = Color(0xFF66758A);
  static const unknownBackground = Color(0xFFECEFF4);
}